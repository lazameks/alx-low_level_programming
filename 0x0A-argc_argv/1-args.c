#include "main.h"

/**
 * main - prints number of argument passed to it
 * @argc: count
 * @argv: argument variables
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d\n", argc - 1);
		return (0);
}
