#include "main.h"
/**
 * main - check the code and prints the compiler name
 * @argc: count
 * @argv: argument
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	int i = 0;

	(void)argc;
	printf("%s\n", argv[i]);
	return (0);
}
